// Firmo — CTA Contact: sección estática. No requiere JS propio.
// El parallax de .contact_image y los reveals de .scroll-into-view los maneja
// la hoja compartida /firmo/scripts/scroll-reveal.js (global, self-init, sin export).
// Stub intencional para mantener el formato de 4 archivos.
