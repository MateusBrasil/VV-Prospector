/* Luzen — Projects List
   Sin lógica local. Las animaciones corren desde /luzen/scripts/animations.js:
   - reveals [data-reveal] vía GSAP + ScrollTrigger (heading [hero="heading"] +
     descripción [hero="description"] + cada card y bloque de info de la grilla)
   - .img-parallax → desplazamiento parallax de las imágenes de cada proyecto al
     hacer scroll. No se registra GSAP aquí: lo hace el motor compartido. */
