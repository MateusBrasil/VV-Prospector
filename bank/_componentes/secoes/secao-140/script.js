/* Upmind — Blogs Hero.
   No section-specific boot code is needed. The load-in choreography (eyebrow,
   h1 SplitText reveal, description fade, featured-card stagger, image zoom) is
   driven by the shared /upmind/scripts/hero-load.js, which targets
   `.section_blogs-hero`. The navbar (sticky + mobile menu) and the button mask
   hover are wired by /upmind/scripts/navbar.js and /upmind/scripts/button-hover.js.
   This file is intentionally an empty classic <script> boot stub. */
