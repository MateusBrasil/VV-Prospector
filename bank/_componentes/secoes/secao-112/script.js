// upmind-hero — classic script (no import/export).
// All behavior (hero reveal, floating badges, navbar, button hover, video
// background) is handled by the shared scripts loaded from /upmind/scripts/.
// Intentionally left as a stub.
