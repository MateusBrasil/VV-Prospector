/* Genovas — Cases Section
 *
 * No component-specific JS. All motion is driven by the shared
 * /genovas/scripts/scroll-reveal.js classic script:
 *   - auto-reveal (fade + blur) for .cases-text and .cases_visual
 *   - a-25 image reveal (scale 1.5 -> 1, blur 10 -> 0) on .img-wrapper .img
 *   - a-62 rotate (0deg -> 120deg) on .cases_img.is-three
 * It self-initializes on DOM ready and sets the hidden start state itself,
 * so content stays visible if GSAP fails to load.
 */
