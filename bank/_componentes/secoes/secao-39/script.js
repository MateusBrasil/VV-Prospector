/* coverly-cta — component-scoped boot logic.
 *
 * The shared /coverly/scripts/scroll-reveal.js auto-initializes on
 * DOMContentLoaded and handles any [data-reveal], [data-reveal-slide],
 * [data-reveal-image], and [fs-numbercount-end] elements — plus the
 * .scroll-slide-left global pattern used on this CTA's .cta_wrap.
 *
 * The button hover micro-interaction is part of the shared Coverly CSS
 * (no JS needed here for this section).
 */
