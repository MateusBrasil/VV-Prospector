// Advisora — Contact Section
// No component-specific boot logic needed. The shared scroll-reveal script
// (/advisora/scripts/scroll-reveal.js) reads the [data-reveal] attributes and
// drives the entrance animation on its own once the DOM is ready.
