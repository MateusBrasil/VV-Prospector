// upmind-cta-section — classic <script>, NO import/export.
//
// All behavior is provided by the shared template scripts loaded before this file:
//   /upmind/scripts/scroll-reveal.js  -> slides the .cta_content (data-reveal) up on enter
//   /upmind/scripts/button-hover.js   -> per-letter slide + press on the two CTA buttons
//
// The background is a native autoplay/loop/muted/playsinline <video> inside a
// Webflow w-background-video, so it plays on its own with no JS. Nothing to boot here.
