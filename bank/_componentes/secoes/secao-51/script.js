// aurae-philosophy-section — no section-specific boot needed.
// The heading/description SplitText reveals (animation="section-heading" /
// "section-description") and the .scroll-into-view item fade-ups are all handled by
// the shared /aurae/scripts/reveal.js. GSAP + ScrollTrigger + SplitText globals load
// in the head. reveal.js sets the hidden start-state, so content stays visible if JS fails.
