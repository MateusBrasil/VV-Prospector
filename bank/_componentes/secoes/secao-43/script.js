/* hirely-cta — no section-specific JS.
 *
 * All animation is owned by the shared engine script
 * `/hirely/scripts/scroll-reveal.js`:
 *   - `.scroll-animation` grow-in reveal (heading block + button wrapper).
 *   - `initCtaImages()` grows each `.cta_img` inside `.cta_header` from
 *     width 0 → natural width (.7s inOutCubic), with a reduced-motion fail-safe.
 *
 * This file is an intentional empty stub (no top-level `export`) so the section
 * loads cleanly as a classic <script src>. */
