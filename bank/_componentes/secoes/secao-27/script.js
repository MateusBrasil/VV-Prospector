/* hirely-faq — no section-specific logic. All behavior lives in the shared engine:
   - /hirely/scripts/accordion.js  → single-open accordion (height anim + arrow rotate, ARIA, keyboard)
   - /hirely/scripts/scroll-reveal.js → .scroll-animation reveals (header, items, faq_cta)
   Loaded as classic <script src> in index.html, so no top-level export here (Rule 1). */
