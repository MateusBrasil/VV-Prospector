/**
 * Genovas — Testimonials Grid Section
 *
 * No component-specific logic. The shared /genovas/scripts/scroll-reveal.js
 * auto-tags .testimonials_card, .author_content, .author_name, .text-* and
 * the .text-wrap heading, then runs the master fade + blur reveal on scroll.
 * It also self-initializes on DOM ready and sets the hidden start-state via
 * GSAP, so content stays visible if JS fails.
 */
