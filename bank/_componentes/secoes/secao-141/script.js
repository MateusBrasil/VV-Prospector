/* Upmind — About Hero
   Classic <script> (NOT a module): no import/export.
   All animation is handled by the shared global scripts loaded before this file
   (hero-load.js, navbar.js, button-hover.js). The hero reveal hooks live in the
   markup/CSS — this file is an intentional stub left for component-specific
   behavior. Keep it free of import/export so it parses as a classic script. */
