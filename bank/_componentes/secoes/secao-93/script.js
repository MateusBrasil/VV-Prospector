// hirely-contact-hero — no local logic. The on-load entrance (bounce-enter ladder:
// h1 SplitText + description + decorative SVG + the three contact-data rows) is
// driven by /hirely/scripts/hero-enter.js; the form's `.scroll-animation` reveal by
// /hirely/scripts/scroll-reveal.js; the navbar by navbar.js + navbar-scroll.js.
// The contact form is a static stub (`onsubmit="return false;"`) — no backend, no
// client validation script needed. Stub kept for the 4-file convention.
