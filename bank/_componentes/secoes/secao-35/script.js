// Aurae — Banner Cta
// No section-specific JS. The dark CTA band's only animation is the heading
// reveal, driven by the shared /aurae/scripts/reveal.js:
//   [animation="section-heading"] is SplitText-split into chars that rise from
//   y 50% + fade in (stagger amount .3) when the section scrolls into view, once.
// The button's fill/hover is pure CSS (.button-component in /aurae/styles.css).
// With prefers-reduced-motion the heading renders in its static, fully-visible state.
