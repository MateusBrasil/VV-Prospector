/* Congra — CTA
   Sin lógica local. Los reveals on-scroll (.slide_down en la card .cta
   y .slide-left en el heading + botón) los maneja el motor compartido
   /congra/scripts/animations.js (GSAP + ScrollTrigger desde CDN).
   El hover del botón (.button_banner-text / .button_cover) es CSS puro
   del cascade /congra/styles.css. */
