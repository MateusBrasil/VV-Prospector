/* Luzen — Services Projects
   Sin lógica local. Las animaciones corren desde /luzen/scripts/animations.js:
   - reveals [data-reveal] vía GSAP + ScrollTrigger (el .project_title de cada
     una de las 3 tarjetas al entrar al viewport)
   - .img-appear → aparición de cada imagen; .img-parallax → su desplazamiento
     parallax ligado al scroll. No se registra GSAP aquí: lo hace el motor
     compartido. */
