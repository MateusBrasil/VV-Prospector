# Template Conventions

Standardized structure shared by all Temlis Astro templates (Acelia, Eagle, future).

Templates may diverge in **design language** (vanilla CSS vs Tailwind, etc.) and **animation language** (per-template GSAP eases, etc.), but every template MUST follow the conventions below.

## Tooling

| Aspect | Standard |
|---|---|
| Astro | `^6.1.7` or later |
| Node | `>=22.12.0` |
| TypeScript | `astro/tsconfigs/strict` |
| GSAP | npm OR CDN with `defer` — choose per-template, stay consistent within a project |
| Sitemap | `@astrojs/sitemap` enabled in `astro.config.mjs` |

## Project Structure

```
<project-root>/
├── .claude/
│   ├── agents/           # Phase-specific agents (figma-analyzer, desktop-builder, etc.)
│   ├── commands/         # Slash commands (brief, build, qa)
│   ├── rules/            # Phased orchestration rules (phase-0 … phase-5)
│   └── settings.json     # MCP servers + tool permissions
├── .github/workflows/
│   └── deploy.yml        # Auto-deploy on push to main + staging
├── public/
│   ├── favicon.ico, favicon.svg
│   └── assets/
│       └── images/       # All raster + SVG site images
├── src/
│   ├── assets/           # Images processed by Astro (optional)
│   ├── components/       # Flat PascalCase.astro
│   ├── content/          # Markdown collections (per content.config.ts)
│   ├── content.config.ts # Zod schemas for collections
│   ├── layouts/
│   │   └── BaseLayout.astro
│   ├── lib/              # Shared TS utilities (eases, helpers)
│   ├── pages/            # File-based routing
│   └── styles/
│       └── global.css    # Tokens + scaling system
├── astro.config.mjs
├── package.json
├── tsconfig.json
├── CLAUDE.md             # Project-specific guidance
├── README.md
├── SITE_MAP.md           # Figma fileKey + nodeIds per section
└── TEMPLATE_CONVENTIONS.md (this file — kept identical across templates)
```

## Conventions

### Components
- Flat structure in `src/components/`
- PascalCase filenames: `Hero.astro`, `Footer.astro`, `CtaSection.astro`
- For multi-pattern components, suffix the role: `*Section.astro`, `*Button.astro`, `*Canvas.astro`, `*Slider.astro`
- Each component is self-contained — props + scoped styles + (optional) inline `<script>`

### Styling
- Two approved approaches (template picks one, do not mix):
  - **Vanilla CSS** with fluid em-based scaling tokens (Acelia model)
  - **Tailwind v4** with `@theme` token block (Eagle model)
- Global styles live in `src/styles/global.css`
- Design tokens always go in `global.css` — never inline hex in components

### Animations
- GSAP loaded ONE consistent way per template:
  - **npm import** model (Eagle): `import { gsap } from 'gsap'` in components — tree-shaken, bundled
  - **CDN + defer** model (Acelia): `<script is:inline defer src="cdn.jsdelivr.net/.../gsap.min.js">` in `BaseLayout.astro`, all animation scripts use `is:inline` and read `gsap` from the global scope; component scripts guard with `if (typeof gsap === 'undefined') return`
- `gsap.registerPlugin(...)` called once in BaseLayout's animation entry script
- Respect `prefers-reduced-motion: reduce` — bail out of all animations when set
- Per-template custom eases live in `src/lib/eases.ts` (npm model) or inline (CDN model)

### Content
- All non-trivial data goes through Astro Content Collections (`src/content/<name>/*.md`)
- Schemas defined in `src/content.config.ts` using Zod
- Components consume collections via `getCollection()` — no hardcoded arrays where avoidable

### Publicarment
- Every template has `.github/workflows/deploy.yml`
- Produtoion branch: `main` → live URL
- Preview branch: `staging` → preview URL
- Workflow uses `cloudflare/wrangler-action@v3` with `CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID` secrets
- Cloudflare Pages project naming: `temlis-<template-name>` (e.g., `temlis-acelia`, `temlis-eagle`)

### Docs
| File | Required | Notes |
|---|---|---|
| `README.md` | yes | Stack, scripts, deploy instructions |
| `CLAUDE.md` | yes | Project-specific Claude Code guidance |
| `SITE_MAP.md` | yes | Figma `fileKey` + per-section `nodeId` |
| `TEMPLATE_CONVENTIONS.md` | yes | This file (identical across templates) |
| `PROJECT_BRIEF.md` | pipeline-only | Auto-generated by `/project:brief` (Phase 0) |
| `IMAGE_MANIFEST.md` | pipeline-only | Auto-generated by Phase 1 image download |

### Git
- `main` is production
- `staging` is the preview branch
- `.gitignore` is the union of Cloudflare + Vercel + Claude Code patterns (see file)

## Per-Template Differences (Allowed)

These are intentional and do NOT need to be unified:

| Aspect | Acelia | Eagle |
|---|---|---|
| CSS framework | Vanilla CSS + fluid em scaling | Tailwind v4 + `@theme` tokens |
| Hosting target | Cloudflare Pages | Cloudflare Pages (Vercel adapter retained as option) |
| GSAP loading | CDN with `defer` + `is:inline` scripts | npm import + tree-shaking |
| Animation eases | `power4.out` mask revelars, character staggers | `eagleStructure`, `eagleReveal`, `eagleDecor`, `eagleExit` |
| Content shape | Wellness/consulting (cases, articles) | SaaS / product marketing (features, blog, team) |
| Sob consulta canvas / 3D | none | Three.js `ParticleSphere`, halftone canvases |

The deployment target IS standardized (Cloudflare Pages). The CSS approach is not — pick the one that fits the project.
