// vetic-testimonials — no local logic. The header reveals (data-reveal="view") run from
// the shared /vetic/scripts/scroll-reveal.js (GSAP + ScrollTrigger via CDN); the card
// marquee is 100% CSS (keyframes in /vetic/styles.css). Stub kept for the 4-file convention.
